
# 🧩 PrimitiveDB — Мини-база данных на Python

## 🗂 Простая учебная консольная СУБД с поддержкой таблиц, CRUD-операций, кэширования и декораторов.

### 🎥 Demo

Посмотреть запись работы в терминале можно здесь:
👉 [![asciinema](https://asciinema.org/a/PFSRry3zOuX10CJeSk1gNSbVx.svg)](https://asciinema.org/a/PFSRry3zOuX10CJeSk1gNSbVx)

[Посмотреть запись работы в терминале на asciinema.org](https://asciinema.org/a/PFSRry3zOuX10CJeSk1gNSbVx)



#### 📦 Структура проекта
```
project2_Nosulchak_Elena_dpo_nod/
├── src/
│   ├── primitive_db/
│   │   ├── __init__.py
│   │   ├── core.py          # Логика таблиц и CRUD
│   │   ├── engine.py        # Главный цикл и парсер команд
│   │   ├── utils.py         # Работа с JSON и метаданными
│   │   ├── parser.py        # Разбор условий WHERE / SET
│   │   └── main.py          # Точка входа
│   └── decorators.py        # Все декораторы проекта
├── data/
│   ├── users.json           # Пример таблицы пользователей
│   └── products.json        # Пример таблицы товаров
├── db_meta.json             # Метаданные таблиц
└── pyproject.toml           # Конфигурация проекта

```

#### ⚙️ Установка

Клонируйте репозиторий:
```
git clone <repo_url>
cd project_name
```

#### Установите зависимости:
```
pip install -r requirements.txt
```

#### Установите пакет локально:
```
pip install .
```

Теперь команда database доступна в терминале.

### 🚀 Запуск
database


После запуска появится интерфейс командной строки:
```
** Primitive DB **
<command> create table <имя> <колонка: тип>... - создать таблицу
<command> list tables                     - показать список таблиц
<command> drop table <имя_таблицы>        - удалить таблицу
<command> exit                            - выйти из программы
<command> help                            - справочная информация
```
#### 🧠 Основные команды
```
Команда	Описание
create table users name:str age:int active:bool	Создать таблицу
list tables	Показать все таблицы
insert users "Alice" 30 true	Добавить запись
select users	Вывести все записи
select users where age = 30	Фильтрация
update users set age = 31 where name = "Alice"	Обновить запись
delete users where name = "Bob"	Удалить запись
drop table users	Удалить таблицу
help	Показать справку
exit	Выйти из программы
```
#### 🧩 Архитектура

Модули:
```
core.py — CRUD (Create, Read, Update, Delete)

engine.py — Цикл ввода команд и маршрутизация

utils.py — Работа с JSON-файлами и метаданными

parser.py — Разбор строковых условий WHERE и SET

decorators.py — Общие декораторы:

@handle_db_errors — обработка ошибок

@confirm_action — подтверждение опасных действий

@log_time — замер времени выполнения

create_cacher() — кэширование результатов SELECT
```
#### 🧪 Проверка проекта
Линтинг:
```
make lint
```

Тестовый запуск:
```
database
```

#### Пример работы:
```
>>>Введите команду: list tables
Таблицы: users, products

>>>Введите команду: select users
+----+----------+-----+--------+
| ID |   name   | age | active |
+----+----------+-----+--------+
| 1  | Alice    | 30  | True   |
| 2  | Bob      | 25  | False  |
+----+----------+-----+--------+
```
#### 🧰 Технологии
```
Python 3.11+

JSON (вместо SQL)

PrettyTable — форматированный вывод

asciinema — запись CLI-демо

Декораторы и замыкания для обработки ошибок, кэширования и логирования
```
🧾 Лицензия

MIT © 2025 — Учебный проект PrimitiveDB
